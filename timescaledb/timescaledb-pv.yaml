apiVersion: v1
kind: PersistentVolume
metadata:
  name: timescaledb-pv
  labels:
    type: local
    app: timescaledb
spec:
  storageClassName: manual
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/timescaledb
